#include <stdio.h>
/**
* main - Entry point
* Description: This is a program that prints all single digit.
* Return: Always 0 (success)
*/

int main(void)
{
	int b;

	b = 0;
	while
		(b < 10) {
			putchar(b + '0');
			b++;
		}
	putchar('\n');
	return (0);
}
