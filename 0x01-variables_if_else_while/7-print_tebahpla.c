#include <stdio.h>

/**
* main - Entry point
* Description: This is a program that prints the lowercase alphabet.
* Return: Always 0 (success)
*/

int mmain(void)
{
	char c;

	c = 'z';
	while
		(c >= 'a') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
